/// @description Init key variables, inc bullet count

//Direction that the bullet will move when created
dir = oPlayer.currentDirection;
//Horizontal speed
hSpd = 11;
//Tracker for active frame count. At 100, the bullet destroys itself
activeFrames = 0;

